# 光谱指数 / MNDWI

## 功能说明

根据绿光与短波红外 1 波段计算 MNDWI，适合增强水体与建筑物、裸地之间的区分度。

## 输入与输出
- 输入：多波段栅格
- 输出：MNDWI 栅格，建议 `.tif`

## 参数

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 输入文件 | 文件路径 | 是 | 无 | 输入多波段影像 |
| 输出文件 | 文件路径 | 是 | 无 | 输出 MNDWI 结果 |
| 绿光波段 | 整数 | 否 | `2` | 绿光波段序号 |
| 短波红外1波段 | 整数 | 否 | `5` | SWIR1 波段序号 |

## 输出说明

- 计算公式：`(Green - SWIR1) / (Green + SWIR1)`
- 输出元数据会返回指数最小值、最大值

## 推荐输出文件名
- `mndwi_result.tif`

## 注意事项

- 如果输入影像没有 SWIR1 波段，则不能直接使用该指数
