# 栅格工具 / COG 生成

## 功能说明

将输入栅格转换为 Cloud Optimized GeoTIFF，便于云端分块读取与快速浏览。

## 输入与输出
- 输入：栅格影像
- 输出：COG 栅格，建议 `.tif`

## 参数

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 输入文件 | 文件路径 | 是 | 无 | 输入栅格影像 |
| 输出文件 | 文件路径 | 是 | 无 | 输出 COG 文件 |

## 输出说明

- 输出结果为 GeoTIFF
- 适合后续用于 Web 分发、云存储或高效浏览

## 推荐输出文件名
- `output_cog.tif`

## 注意事项

- 输出文件通常比原始普通 TIFF 更适合范围读取
- 如果输入文件本身质量较差，转换不会自动修复数据问题
