# 地形分析 / TPI

## 功能说明

按 `3x3` 邻域计算地形位置指数，用于描述像元相对周边地形的高低关系。

## 输入与输出

- 输入：DEM 栅格
- 输出：TPI 结果栅格，建议 `.tif`

## 参数

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 输入文件 | 文件路径 | 是 | 无 | DEM 输入 |
| 输出文件 | 文件路径 | 是 | 无 | 输出 TPI 栅格 |
| 波段号 | 整数 | 否 | `1` | DEM 波段 |
| z_factor | 浮点数 | 否 | `1.0` | 高程缩放系数 |

## 注意事项

- 当前实现使用固定 `3x3` 邻域。
- 输出值大于 `0` 通常表示相对周边偏高，小于 `0` 表示相对周边偏低。

## 推荐输出文件名

- `terrain_tpi_result.tif`
