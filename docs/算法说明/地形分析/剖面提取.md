# 地形分析 / 剖面提取

## 功能说明

沿给定折线路径采样 DEM 高程，并导出剖面 CSV。

## 输入与输出

- 输入：DEM 栅格
- 输出：剖面采样结果，建议 `.csv`

## 参数

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 输入文件 | 文件路径 | 是 | 无 | DEM 输入 |
| 输出文件 | 文件路径 | 是 | 无 | 输出剖面 CSV |
| 波段号 | 整数 | 否 | `1` | DEM 波段 |
| profile_path | 字符串 | 是 | 无 | 剖面路径，格式为 `x1,y1;x2,y2;...` |

## 输出说明

输出 CSV 表头固定为：

- `distance`
- `x`
- `y`
- `elevation`

## 注意事项

- `profile_path` 至少需要两个点。
- 输出文件应使用 `.csv`。
- 路径坐标使用栅格空间参考下的地图坐标，而不是像素坐标。

## 推荐输出文件名

- `terrain_profile_result.csv`
